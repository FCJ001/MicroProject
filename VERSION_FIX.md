# 版本修复说明

## 问题

`@micro-zoe/micro-app@^1.0.0` 版本不存在，最新版本是 `1.0.0-rc.28`

## 已修复

已更新 `main-app/package.json` 中的版本号：
- 从 `^1.0.0` 
- 改为 `^1.0.0-rc.28`

## 现在请运行

```bash
pnpm install
```

或者直接运行：

```bash
./start.sh
```

脚本会自动安装正确版本的依赖。
